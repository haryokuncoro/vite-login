# copy to .env and edit
PORT=4000
DATABASE_FILE=./data/db.sqlite
JWT_SECRET=replace_this_with_a_strong_random_secret
JWT_EXPIRES_IN=1h

# nodemailer SMTP settings (optional). If not set, server prints mock emails to console.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
FROM_EMAIL="no-reply@example.com"
